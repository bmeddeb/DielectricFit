"""Django models for the library app (reserved).

At present, the library app serves algorithmic code and a dielectric model registry.
No database models are required yet, but this module is kept for future extensions.
"""

from __future__ import annotations

# Intentionally empty for now.

